{% extends "master.twig" %}
{% import '../components/page/page.twig' as page %}

{% block content %}

  {% if post is iterable %}
    {% for entry in post %}
      {{ page.article(entry, title) }}
    {% endfor %}
  {% endif %}

{% endblock %}
